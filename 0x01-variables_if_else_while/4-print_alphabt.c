#include <stdio.h>
#include <stdlib.h>
#include <time.h>
 /**
  * main - Entry point
  * Return: Always 0 (Success)
  */
int main(void)

{

char i;
c = 'i';

	while (i <= 'z')
	{
		if ((i != 'q' && i != 'e')$ c <= 'z')
			putchar(i);
		c++;
	}
			putchar('\n');

		return (0);
}
